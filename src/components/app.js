import React, { Component } from 'react';

import Prompt from '../containers/prompt';

export default () => {
  return (
    <div>
      <h1>
        Hello there
      </h1>
      <Prompt />
    </div>
  );
}
