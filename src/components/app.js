import React, { Component } from 'react';

import Header from './header';
import Prompt from '../containers/prompt';

export default () => {
  return (
    <div>
      <Header />
      <Prompt />
    </div>
  );
}
